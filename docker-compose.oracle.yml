services:
  app:
    build: .
    container_name: atlas-markets-app
    restart: unless-stopped
    env_file:
      - .env.oracle
    ports:
      - "127.0.0.1:8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: ["/bin/sh", "-c", "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --proxy-headers --forwarded-allow-ips='*'"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health')"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - atlas-markets-private

  postgres:
    image: postgres:17-alpine
    container_name: atlas-markets-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: atlas_markets
      POSTGRES_USER: atlas
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U atlas -d atlas_markets"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - atlas-markets-postgres-data:/var/lib/postgresql/data
      - ./backups:/backups
    networks:
      - atlas-markets-private

  redis:
    image: redis:7-alpine
    container_name: atlas-markets-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - atlas-markets-redis-data:/data
    networks:
      - atlas-markets-private

networks:
  atlas-markets-private:
    name: atlas-markets-private

volumes:
  atlas-markets-postgres-data:
  atlas-markets-redis-data:
