/* ATLAS v65.1 — broker-style charts for active v61 Open Positions. Read-only. */
(()=>{'use strict';const TF=['1m','5m','15m','1h','4h','1d'],N=v=>Number(v),money=v=>Number.isFinite(N(v))?`$${N(v).toFixed(2)}`:'—',norm=r=>(r||[]).map(x=>({t:N(x.time??x.timestamp??x.start??x.open_time),o:N(x.open),h:N(x.high),l:N(x.low),c:N(x.close),v:N(x.volume||0)})).filter(x=>[x.o,x.h,x.l,x.c].every(Number.isFinite));function sma(d,n){return d.map((_,i)=>i<n-1?null:d.slice(i-n+1,i+1).reduce((a,x)=>a+x.c,0)/n)}function draw(canvas,rows,entry){const d=norm(rows),ctx=canvas.getContext('2d'),r=canvas.getBoundingClientRect(),D=devicePixelRatio||1,w=Math.max(360,r.width),h=Math.max(300,r.height);canvas.width=w*D;canvas.height=h*D;ctx.scale(D,D);ctx.clearRect(0,0,w,h);if(d.length<3){ctx.fillStyle='#8392a7';ctx.font='12px sans-serif';ctx.fillText('Candles unavailable',18,28);return}const P={l:12,r:62,t:30,b:44},vh=52,ph=h-P.t-P.b-vh,hi=Math.max(...d.map(x=>x.h),N(entry)||-Infinity),lo=Math.min(...d.map(x=>x.l),N(entry)||Infinity),span=hi-lo||1,y=v=>P.t+(hi-v)/span*ph,x=i=>P.l+(i+.5)*(w-P.l-P.r)/d.length,step=(w-P.l-P.r)/d.length,cw=Math.max(2,Math.min(8,step*.66)),vmax=Math.max(...d.map(x=>x.v),1);ctx.strokeStyle='rgba(125,150,178,.13)';for(let i=0;i<5;i++){const yy=P.t+i*ph/4;ctx.beginPath();ctx.moveTo(P.l,yy);ctx.lineTo(w-P.r,yy);ctx.stroke();ctx.fillStyle='#8290a4';ctx.font='10px sans-serif';ctx.fillText((hi-i*span/4).toFixed(2),w-P.r+8,yy+3)}d.forEach((q,i)=>{const xx=x(i),up=q.c>=q.o,col=up?'#24c98a':'#ef5261';ctx.strokeStyle=col;ctx.fillStyle=col;ctx.beginPath();ctx.moveTo(xx,y(q.h));ctx.lineTo(xx,y(q.l));ctx.stroke();ctx.fillRect(xx-cw/2,Math.min(y(q.o),y(q.c)),cw,Math.max(1,Math.abs(y(q.o)-y(q.c))));const vb=q.v/vmax*vh;ctx.globalAlpha=.36;ctx.fillRect(xx-cw/2,h-P.b-vb,cw,vb);ctx.globalAlpha=1});[[9,'#3182f6'],[20,'#ef8f28']].forEach(([n,col])=>{const m=sma(d,n);ctx.strokeStyle=col;ctx.lineWidth=1.2;ctx.beginPath();let s=false;m.forEach((v,i)=>{if(v==null)return;s?ctx.lineTo(x(i),y(v)):(ctx.moveTo(x(i),y(v)),s=true)});ctx.stroke()});if(Number.isFinite(N(entry))){const yy=y(N(entry));ctx.setLineDash([5,5]);ctx.strokeStyle='rgba(215,225,238,.52)';ctx.beginPath();ctx.moveTo(P.l,yy);ctx.lineTo(w-P.r,yy);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='#aab6c7';ctx.font='9px sans-serif';ctx.fillText(`ENTRY ${money(entry)}`,P.l+5,Math.max(11,yy-5))}const last=d.at(-1),yy=y(last.c),col=last.c>=last.o?'#24c98a':'#ef5261';ctx.strokeStyle=col;ctx.setLineDash([2,3]);ctx.beginPath();ctx.moveTo(P.l,yy);ctx.lineTo(w-P.r,yy);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle=col;ctx.fillRect(w-P.r+2,yy-9,58,18);ctx.fillStyle='#06131d';ctx.font='bold 10px sans-serif';ctx.fillText(last.c.toFixed(2),w-P.r+7,yy+3);ctx.fillStyle='#9ba9bc';ctx.font='10px sans-serif';[0,Math.floor(d.length/2),d.length-1].forEach(i=>{const dt=new Date(d[i].t>1e12?d[i].t:d[i].t*1000);if(!isNaN(dt))ctx.fillText(dt.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}),Math.max(P.l,Math.min(w-P.r-36,x(i)-18)),h-9)})}async function upgrade(card,p){if(card.dataset.v651==='1')return;card.dataset.v651='1';const host=card.querySelector('.v61-chart');if(!host)return;host.classList.add('v65-host');const controls=document.createElement('div');controls.className='v65-controls';controls.innerHTML=TF.map(t=>`<button class="v65-tf ${t==='5m'?'active':''}" data-tf="${t}">${t}</button>`).join('')+'<span class="v65-ind">MA 9 · MA 20 · Volume</span>';host.before(controls);const load=async tf=>{try{const res=await api(`/portfolio-market/${encodeURIComponent(p.profile_id)}/${encodeURIComponent(p.symbol)}/candles?timeframe=${encodeURIComponent(tf)}&limit=120`),rows=res.candles||res.list||[],d=norm(rows),last=d.at(-1);host.innerHTML='<div class="v65-ohlc"></div><canvas class="v65-canvas"></canvas>';if(last)host.querySelector('.v65-ohlc').innerHTML=`O <b>${last.o.toFixed(2)}</b> H <b>${last.h.toFixed(2)}</b> L <b>${last.l.toFixed(2)}</b> C <b>${last.c.toFixed(2)}</b>`;draw(host.querySelector('canvas'),rows,p.entry_price)}catch(e){host.innerHTML='<div class="empty-state muted">Chart unavailable</div>'}};controls.querySelectorAll('button').forEach(b=>b.onclick=()=>{controls.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));load(b.dataset.tf)});await load('5m')}async function run(){if(typeof api!=='function')return;try{const data=await api('/portfolio'),ps=data.positions||[],cards=[...document.querySelectorAll('.v61-holding')];for(const card of cards){const symbol=card.querySelector('.v61-symbol')?.textContent?.trim(),p=ps.find(x=>String(x.symbol).toUpperCase()===String(symbol).toUpperCase());if(p)await upgrade(card,p)}}catch(e){console.warn('v65.1',e)}}const prior=renderPage;renderPage=async page=>{const r=await prior(page);if(page==='Portfolio'){setTimeout(run,120);setTimeout(run,500);setTimeout(run,1200)}return r};window.AtlasOpenPositionChartsV65={run};})();